version: '3.8'

services:
  mongodb:
    image: mongo:8.0.15
    container_name: pdf-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password123
    volumes:
      - mongodb_data:/data/db
      - ./scripts/mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    networks:
      - pdf-network

  postgres:
    image: postgres:16
    container_name: pdf-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: pdf_auth
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/postgres-init.sql:/docker-entrypoint-initdb.d/postgres-init.sql:ro
    networks:
      - pdf-network

  # app:
  #   build: 
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: pdf-backend
  #   restart: unless-stopped
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     NODE_ENV: development
  #     MONGODB_URI: mongodb://admin:password123@mongodb:27017/pdf_templates?authSource=admin
  #     POSTGRES_URI: postgresql://admin:password123@postgres:5432/pdf_auth
  #     JWT_SECRET: tu_jwt_secret_super_seguro_para_desarrollo
  #     JWT_REFRESH_SECRET: tu_refresh_secret_para_desarrollo
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #   depends_on:
  #     - mongodb
  #     - postgres
  #   networks:
  #     - pdf-network
  #   command: npm run dev

volumes:
  mongodb_data:
  postgres_data:

networks:
  pdf-network:
    driver: bridge